5 CLS
6 A = PEEK(&HFFFE)*256+PEEK(&HFFFF)
7 IF A = &H8C1B THEN RGB
10 ? "COLOR COMPUTER KEYBOARD TEST    PROGRAMS"
12 ?
14 ? "BY TIM LINDNER"
14 ? "TLINDNER.MACMESS.ORG"
16 ?
20 ? " 1 - FORWARD TEST"
30 ? " 2 - BACKWARD TEST"
40 ? " 3 - CC3 INTERRUPT TEST"
45 A$ = "NONE"
50 INPUT A
60 IF A = 1 THEN A$="KT-F/BIN"
70 IF A = 2 THEN A$="KT-B/BIN"
80 IF A = 3 THEN A$="KT-CC3/BIN"
90 IF A$ = "NONE" THEN 5
100 LOADM A$
105 POKE &HFF40,0 : REM TURN OFF FLOPPY
110 EXEC
